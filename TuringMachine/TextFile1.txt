print:
            /*
            foreach (Cell c in cells) {
                Console.Write('|');
                Console.Write(Cell.A[c.get()]);
            }
            Console.Write('|');
            Console.Write('\t');
            */            /*
            var symbols = cells.Cast<Cell>().Select(c => Cell.A[c.get()]);
            Console.Write("|" + string.Join("|", symbols) + "|\t");
            */
printHead:
            /*
            foreach (Cell c in cells) {
                Console.Write(' ');
                Console.Write((c == curr)?'*':' ');
            }
            Console.WriteLine();
            */

            /*
            String s = new String(' ',cells.Count).Remove(pos,1).Insert(pos,"*");
            Console.WriteLine(' ' + String.Join(" ",s.ToCharArray()));
            */
